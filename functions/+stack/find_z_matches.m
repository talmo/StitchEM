function matches = find_z_matches(secA, secB, featsA, featsB, parameters)
%FIND_Z_MATCHES Detects matching points across a pair of sections.

% Loop through tiles
% Call processing.match_features()
% Save to cache

end

